import type { MedusaRequest, MedusaResponse } from "@medusajs/framework/http"
export async function GET(req:MedusaRequest,res:MedusaResponse){const spike=req.scope.resolve("spike") as any;const status=String((req.query as any)?.status||"pending");const revisions=await spike.listSpikeProductRevisions(status==="all"?{}:{status},{take:500,order:{created_at:"DESC"}});res.json({revisions,count:revisions.length})}
