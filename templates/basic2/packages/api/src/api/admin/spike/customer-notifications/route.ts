import type { MedusaRequest,MedusaResponse } from "@medusajs/framework/http"
export async function GET(req:MedusaRequest,res:MedusaResponse){const spike=req.scope.resolve("spike") as any;const notifications=await spike.listSpikeNotifications({audience:"customer"},{take:100,order:{created_at:"DESC"}});res.json({notifications})}
export async function POST(req:MedusaRequest,res:MedusaResponse){const b=(req.body||{}) as any;const title=String(b.title||"").trim(),body=String(b.body||"").trim();if(!title)return res.status(400).json({message:"اكتب عنوان الإشعار"});const spike=req.scope.resolve("spike") as any;const n=await spike.createSpikeNotifications({audience:"customer",customer_id:b.customer_id?String(b.customer_id):null,type:"admin_message",title,body:body||null,entity_type:b.route?"route":null,entity_id:b.route?String(b.route):null,read:false});res.status(201).json({notification:n})}

export async function DELETE(req:MedusaRequest,res:MedusaResponse){const spike=req.scope.resolve("spike") as any;const rows=await spike.listSpikeNotifications({audience:"customer",type:"admin_message"},{take:1000});for(const n of rows)await spike.deleteSpikeNotifications(n.id);res.json({ok:true,deleted:rows.length})}
