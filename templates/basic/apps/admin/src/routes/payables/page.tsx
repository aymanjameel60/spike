import{useEffect,useState}from'react';import type{RouteConfig}from'@mercurjs/dashboard-sdk';import"../../spike-shared/spike-native.css";declare const __BACKEND_URL__:string;export const config:RouteConfig={label:'المستحقات',rank:70};const api=async(p:string,i?:RequestInit)=>{const r=await fetch(`${__BACKEND_URL__}${p}`,{credentials:'include',...i,headers:{...(i?.body?{'content-type':'application/json'}:{}),...(i?.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`HTTP ${r.status}`);return d};export default function P(){const[x,setX]=useState<any[]>([]);const load=()=>api('/admin/spike/payables').then(d=>setX(d.payables||[]));useEffect(()=>{load()},[]);const paid=async(id:string)=>{await api('/admin/spike/payables',{method:'POST',body:JSON.stringify({id,status:'paid'})});load()};return <div className="spike-native-page" dir="rtl"><h1>المستحقات</h1><p>مستحقات التجار ومكاتب التوصيل وديون العملاء بعد المرتجعات.</p><table className="spike-native-table"><thead><tr><th>النوع</th><th>المستفيد</th><th>الطلب</th><th>المبلغ</th><th>الحالة</th><th></th></tr></thead><tbody>{x.map(p=><tr key={p.id}><td>{p.kind==='seller'?'تاجر':p.kind==='office'?'مكتب توصيل':'دين للعميل'}</td><td>{p.beneficiary_name||p.phone||p.beneficiary_id||'—'}</td><td>{p.order_id||'—'}</td><td>{p.amount} {p.currency_code}</td><td>{p.status}</td><td>{p.status!=='paid'&&<button className="spike-native-btn" onClick={()=>paid(p.id)}>تم السداد</button>}</td></tr>)}</tbody></table></div>}
