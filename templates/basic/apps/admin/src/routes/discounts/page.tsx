import{useEffect,useState}from"react";import type{RouteConfig}from"@mercurjs/dashboard-sdk";import"../../spike-shared/spike-native.css";declare const __BACKEND_URL__:string
export const config:RouteConfig={label:"الخصومات",rank:18};
const api=async(p:string,i?:RequestInit)=>{const r=await fetch(`${__BACKEND_URL__}${p}`,{credentials:'include',...i,headers:{...(i?.body?{'content-type':'application/json'}:{}),...(i?.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||`HTTP ${r.status}`);return d}
export default function DiscountsPage(){const[x,setX]=useState<any[]>([]),[msg,setMsg]=useState('');const load=()=>api('/admin/spike/discounts?status=all').then(d=>setX((d.discounts||[]).filter((v:any)=>v.request_type!=='coupon')));useEffect(()=>{load().catch(e=>setMsg(e.message))},[]);const review=async(id:string,action:string)=>{const reason=action==='reject'?(prompt('سبب الرفض')||'تم رفض الطلب'):'';try{await api(`/admin/spike/discounts/${id}`,{method:'POST',body:JSON.stringify({action,reason})});setMsg(action==='approve'?'تم اعتماد الخصم':'تم رفض الخصم');await load()}catch(e:any){setMsg(e.message)}};return <div className="spike-native-page" dir="rtl"><div className="spike-native-card"><h1 className="spike-native-title">الخصومات</h1><p className="spike-native-muted">طلبات خصم المنتجات مستقلة عن الكوبونات.</p>{msg&&<p>{msg}</p>}</div><div className="spike-native-card"><table className="spike-native-table"><thead><tr><th>المنتج</th><th>السعر</th><th>الخصم</th><th>الحالة</th><th>إجراء</th></tr></thead><tbody>{x.map(d=><tr key={d.id}><td>{d.product_title||d.product_id||'—'}</td><td>{d.original_price} ← {d.discounted_price} {d.currency_code}</td><td>{d.percentage}%</td><td>{d.status}</td><td>{d.status==='pending'?<><button className="spike-native-btn" onClick={()=>review(d.id,'approve')}>موافقة</button> <button className="spike-native-btn danger" onClick={()=>review(d.id,'reject')}>رفض</button></>:<span>—</span>}</td></tr>)}</tbody></table>{!x.length&&<div className="spike-native-muted" style={{padding:20}}>لا توجد خصومات.</div>}</div></div>}
