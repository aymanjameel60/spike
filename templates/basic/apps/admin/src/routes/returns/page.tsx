import{useEffect,useState}from'react';import type{RouteConfig}from'@mercurjs/dashboard-sdk';import"../../spike-shared/spike-native.css";declare const __BACKEND_URL__:string;export const config:RouteConfig={label:'المرتجعات',rank:69};export default function P(){const[x,setX]=useState<any[]>([]);useEffect(()=>{fetch(`${__BACKEND_URL__}/admin/spike/returns`,{credentials:'include'}).then(r=>r.json()).then(d=>setX(d.returns||[]))},[]);return <div className="spike-native-page" dir="rtl"><h1>المرتجعات</h1><p>سجل كامل للمرتجعات. قرار قبول/رفض المنتج المرتجع يقوم به التاجر.</p><table className="spike-native-table"><thead><tr><th>الطلب</th><th>المنتج</th><th>التاجر</th><th>المبلغ</th><th>الحالة</th><th>السبب</th></tr></thead><tbody>{x.map(r=><tr key={r.id}><td>{r.order_id}</td><td>{r.product_id}</td><td>{r.seller_id}</td><td>{r.amount} {r.currency_code}</td><td>{r.status}</td><td>{r.rejection_reason||r.reason||'—'}</td></tr>)}</tbody></table></div>}
