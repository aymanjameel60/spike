import{useEffect,useState}from'react';import type{RouteConfig}from'@mercurjs/dashboard-sdk';import{Card,Empty,Page,PageHeader,Status}from'../../spike-shared/ui';import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from'../../components/ui/table';declare const __BACKEND_URL__:string;export const config:RouteConfig={label:'المرتجعات',rank:69};export default function P(){const[x,setX]=useState<any[]>([]);useEffect(()=>{fetch(`${__BACKEND_URL__}/admin/spike/returns`,{credentials:'include'}).then(r=>r.json()).then(d=>setX(d.returns||[]))},[]);return <Page><Card><PageHeader title="المرتجعات" description="سجل كامل للمرتجعات. قرار قبول أو رفض المنتج المرتجع يقوم به التاجر."/></Card><Card>{x.length?<Table><TableHeader><TableRow><TableHead>الطلب</TableHead><TableHead>المنتج</TableHead><TableHead>التاجر</TableHead><TableHead>المبلغ</TableHead><TableHead>الحالة</TableHead><TableHead>السبب</TableHead></TableRow></TableHeader><TableBody>{x.map(r=><TableRow key={r.id}><TableCell>{r.order_id}</TableCell><TableCell>{r.product_id}</TableCell><TableCell>{r.seller_id}</TableCell><TableCell>{r.amount} {r.currency_code}</TableCell><TableCell><Status tone={r.status==='approved'?'success':r.status==='rejected'?'danger':'warning'}>{r.status}</Status></TableCell><TableCell>{r.rejection_reason||r.reason||'—'}</TableCell></TableRow>)}</TableBody></Table>:<Empty>لا توجد مرتجعات.</Empty>}</Card></Page>}
