import type { MedusaRequest, MedusaResponse } from "@medusajs/framework/http"
const required=(b:any,k:string)=>String(b?.[k]||'').trim()
export async function GET(req:MedusaRequest,res:MedusaResponse){const s=req.scope.resolve('spike') as any;res.json({offices:await s.listSpikeDeliveryOffices({}, {take:500})})}
export async function POST(req:MedusaRequest,res:MedusaResponse){const s=req.scope.resolve('spike') as any,b=(req.body||{}) as any;for(const [k,label] of [['name','اسم المكتب'],['governorate','المحافظة'],['city','المدينة'],['phone','رقم التواصل']] as const){if(!required(b,k))return res.status(422).json({message:`${label} مطلوب`,field:k})}const type=['in_city','intercity','both'].includes(String(b.service_type))?String(b.service_type):'in_city';const office=await s.createSpikeDeliveryOffices({name:required(b,'name'),governorate:required(b,'governorate'),city:required(b,'city'),phone:required(b,'phone'),service_type:type,calculation_type:'distance',covered_cities:Array.isArray(b.covered_cities)?b.covered_cities:[],routes:Array.isArray(b.routes)?b.routes:[],latitude:b.latitude==null?null:Number(b.latitude),longitude:b.longitude==null?null:Number(b.longitude),active:b.active!==false});res.status(201).json({office})}
