import React,{useState}from'react';import{createRoot}from'react-dom/client';import{LayoutDashboard,ShoppingBag,Store,Tags,TicketPercent,Truck,Coins,Settings,LogOut}from'lucide-react';import'./style.css';
const API='http://localhost:9100/api/v1';
function App(){const[token,setToken]=useState(localStorage.spike_admin_token||'');const[email,setEmail]=useState('admin@spike.com');const[password,setPassword]=useState('');const[error,setError]=useState('');async function login(e:any){e.preventDefault();setError('');const r=await fetch(API+'/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok||!d.token)return setError(d.error||'بيانات الدخول غير صحيحة');localStorage.spike_admin_token=d.token;setToken(d.token)}if(!token)return <main className="login"><form onSubmit={login}><h1>Spike</h1><h2>لوحة الإدارة</h2><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة المرور"/><button>تسجيل الدخول</button>{error&&<p>{error}</p>}</form></main>;const nav=[[LayoutDashboard,'الرئيسية'],[ShoppingBag,'الطلبات'],[Store,'المتاجر'],[Tags,'الفئات والمنتجات'],[TicketPercent,'الكوبونات والخصومات'],[Truck,'التوصيل'],[Coins,'العملات'],[Settings,'الإعدادات']];return <div className="app" dir="rtl"><aside><div className="brand">Spike</div><nav>{nav.map(([I,t]:any)=><button key={t}><I size={20}/>{t}</button>)}</nav><button className="logout" onClick={()=>{localStorage.removeItem('spike_admin_token');setToken('')}}><LogOut size={20}/>تسجيل الخروج</button></aside><section><header><div><h1>لوحة التحكم</h1><p>إدارة Spike من مكان واحد</p></div><span>Admin</span></header><div className="cards"><article><b>الطلبات</b><strong>—</strong><small>متصل بالباكند الجديد</small></article><article><b>المتاجر</b><strong>—</strong><small>إدارة البائعين</small></article><article><b>المنتجات</b><strong>—</strong><small>إدارة الكتالوج</small></article><article><b>قيد المراجعة</b><strong>—</strong><small>الطلبات والكوبونات</small></article></div><div className="panel"><h2>Spike Admin</h2><p>تم إنشاء الهيكل الجديد المستقل عن Medusa. سنربط الأقسام بالـ API خطوة بخطوة.</p></div></section></div>};createRoot(document.getElementById('root')!).render(<App/>);